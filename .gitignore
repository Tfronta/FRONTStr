__pycache__/
*.py[cod]
*$py.class
*.egg
*.egg-info/
.eggs/
dist/
build/
*.so
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.coverage
.coverage.*
coverage.xml
htmlcov/
.hypothesis/

.venv/
venv/
env/
.python-version

.idea/
.vscode/
*.swp
*.swo
.DS_Store

*.bam
*.bai
*.cram
*.crai
*.vcf
*.vcf.gz
*.vcf.gz.tbi
*.fastq
*.fastq.gz
*.fq.gz
*.fasta
*.fa
*.fa.fai
*.gzi

out/
output/
results/
tmp/
scratch/

.env
.env.*
secrets/
.claude/

# ONT R10 BAM slices for development (not versioned)
tests/data/ont_slices/*.bam
tests/data/ont_slices/*.bai

# The demo sample IS versioned. It is the one dataset a fresh clone can run
# end to end without downloading anything, so it has to survive the *.bam
# rule above. 17 MB, and it reproduces the reference profile exactly.
!demodata/*.bam
!demodata/*.bam.bai

# Internal work plan. Kept local: it carries effort estimates and phase
# numbering that mean nothing outside the project, and it described an
# external-caller phase that is no longer part of the pipeline.
ROADMAP.md
